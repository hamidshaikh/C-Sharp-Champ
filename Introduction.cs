/*
  Here we are using 'System' namespace.
  A namespace is used to organise our code,
  and is collection of classes,interface, structs, enum and delegates etc.
*/
using System;
//'Program' is a name of class which is public in this example.
public class Program
{
	// 'Main' method is a entry point of application, so here application starts from main method.
  public static void Main()
  {
      //Console is a class present in System namespace and has a method 'WriteLine' to print message. i.e. Hello World.
       Console.WriteLine("Hello World"); //
    }
}
